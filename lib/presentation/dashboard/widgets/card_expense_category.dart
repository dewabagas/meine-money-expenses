import 'package:flutter/material.dart';

class CardExpenseCategory extends StatelessWidget {
  const CardExpenseCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
